/** @format */

import React from "react";

function SingleInputField() {
  return (
    <div>
      <input
        type="text"
        // id={props.id} name={props.label} value={props.label}
      />
    </div>
  );
}

export default SingleInputField;
